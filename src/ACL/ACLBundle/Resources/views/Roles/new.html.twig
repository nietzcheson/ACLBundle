{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Roles creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('roles') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
